const { Router } = require('express');

const router = Router();

// Testimonial and Email Template Routes will be mounted here
// router.use('/testimonials', testimonialRoutes);
// router.use('/email-templates', emailTemplateRoutes);

module.exports = router;
